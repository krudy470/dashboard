<!DOCTYPE html>
<html>
  <head>
    <title><%= @title %> | OrionVm</title>
    <%= csrf_meta_tag %>
    <%= javascript_include_tag :application %>
  </head>
  <body>
    <div class="container">
      <%= render 'layouts/header' %>
      <section class="round">
        <%= yield %>
      </section>
      <%= render 'layouts/footer' %>
    </div>
  </body>
</html>
